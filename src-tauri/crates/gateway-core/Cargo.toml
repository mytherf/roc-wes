[package]
name = "gateway-core"
version = "0.1.0"
description = "设备网关领域模型：配置、遥测、错误与 DeviceAdapter 端口（六边形架构核心，无 IO 依赖）"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
async-trait = "0.1"
