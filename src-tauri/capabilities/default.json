{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口默认权限（核心 IPC；自定义命令不受 capability 限制）",
  "windows": ["main"],
  "permissions": ["core:default", "shell:allow-open"]
}
