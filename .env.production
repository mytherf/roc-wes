# ========== 生产构建环境变量 ==========
# 说明：
# - 该文件在 npm run build（生产打包）时自动加载
# - 与 .env.local 不同，它会被提交到代码仓库，适合存放非敏感的公共配置
# - 以 VITE_ 开头的变量会通过 import.meta.env.xxx 暴露给前端代码
# 当前项目暂无生产环境特有的变量，保留此文件作为扩展入口
# 例如（取消注释即可使用）：
# VITE_API_BASE_URL=https://api.example.com
