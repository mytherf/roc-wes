# SSE 数据源 · 开发手册

> 面向维护、调试或扩展 SSE 数据接入的开发者。数据源类型标识：`sse`。

## 一、架构与数据流

SSE 基于 WebView 原生 `EventSource`，**真实模式**下无网关，每个数据点一条独立的 SSE 连接；**演示模式**下则由桌面端 Rust 网关内置 `DemoAdapter`（`profile = Sse`，锯齿波）生成数据，经 Tauri IPC 推送（不占用端口）：

```
真实模式：
┌────────────┐  subscribe(pointId)  ┌─────────────┐  EventSource url?pointId=x  ┌────────────┐
│ X6 节点     │ ───────────────────▶ │ SseService  │ ──────────────────────────▶ │ SSE 接口    │
│ node.data   │                      │ 每点一个      │ ◀════ text/event-stream ═══ │            │
│  .value     │ ◀─────────────────── │ EventSource │   data: {json}\n\n          └────────────┘
└────────────┘   DataPoint 回调       └─────────────┘   （浏览器自动重连）

演示模式（桌面端）：
┌────────────┐  subscribe           ┌──────────────────┐  invoke gateway_connect   ┌──────────────────┐
│ X6 节点     │ ───────────────────▶ │ IpcGatewayService │ ────────────────────────▶ │ Rust DemoAdapter │
│ node.data   │                      │  (演示模式路由)    │ ◀── gateway://telemetry ── │  profile = Sse   │
└────────────┘   DataPoint 回调       └──────────────────┘                            └──────────────────┘
```

## 二、核心类型定义

复用统一的 `DataPoint` / `IDataService` / `DataBindingConfig`（`src/services/DataService.ts`）。数据源实例 `DataSource.type = 'sse'`，`config` 通常为空。

## 三、前端 Service 实现

文件：`src/services/SseService.ts`。

```ts
subscribe(pointId: string, callback: DataCallback): void {
    if (!this.callbacks.has(pointId)) this.callbacks.set(pointId, [])
    this.callbacks.get(pointId)!.push(callback)

    if (!this.sources.has(pointId)) {
        const sep = this.url.includes('?') ? '&' : '?'
        const es = new EventSource(`${this.url}${sep}pointId=${encodeURIComponent(pointId)}`)
        es.onmessage = (event) => {
            const data = JSON.parse(event.data)
            const point: DataPoint = {
                id: data.id || pointId,
                value: data.value ?? data.data,
                timestamp: data.timestamp || Date.now(),
                quality: data.quality || 'good',
            }
            for (const cb of this.callbacks.get(pointId) || []) cb(point)
        }
        es.onerror = () => {
            // EventSource 会自动重连，这里仅记录
            console.warn('[SSE] 连接异常，浏览器将自动重连')
        }
        this.sources.set(pointId, es)
    }
}
```

要点：

- **每点独立 EventSource**：`sources: Map<pointId, EventSource>`；`unsubscribe` 调用 `es.close()` 并移除。
- **自动重连**：由浏览器内建，无需手写重连逻辑；`onerror` 仅记录日志。
- **URL 拼接**：自动判断 `?`/`&` 追加 `pointId` 并编码。
- `disconnect()` 关闭全部 EventSource 并清空回调。

## 四、数据格式与点ID约定

| 方向 | 内容 | 说明 |
| --- | --- | --- |
| 前端 → 接口 | `GET <url>?pointId=<pointId>`（`Accept: text/event-stream`） | 建立 SSE 流 |
| 接口 → 前端 | `data: {"id"?, "value", "timestamp"?, "quality"?}\n\n` | 标准 SSE 帧 |

可选控制帧：`retry: <ms>\n\n`（重连间隔）、`event: <name>\n`（命名事件，当前实现仅监听默认 `message`）。

## 五、内置模拟引擎（演示模式）

Node 版内置模拟接口（原 `mock/server.ts` 的 `startSseServer()`，监听 `http://localhost:8082`）已移除。演示模式改由桌面端 Rust 网关内置实现：`src-tauri/crates/gateway-demo/src/lib.rs` 的 `DemoAdapter`（`profile = Sse`），不监听任何端口、不建立 SSE 连接，由 gateway-engine 会话任务按轮询周期读取后经 Tauri IPC 事件推送。

数据生成算法——锯齿斜升，10 秒周期 0→100 后归零（与原 Node 版 `sseValue()` 行为一致，噪声改由哈希派生以保证确定性）：

```rust
fn sse_value(point_id: &str, now_ms: u64) -> f64 {
    let period: u64 = 10_000;
    let phase = (hash_u64(point_id) % 628) as f64 / 100.0;
    let offset = (phase / (2.0 * std::f64::consts::PI) * period as f64) as u64;
    round1((((now_ms + offset) % period) as f64 / period as f64) * 100.0)
}
```

## 六、路由与节点绑定机制

`useDataService.ts`：演示模式（`isDemoSource` 判定）在 Tauri 桌面运行时统一路由到 `IpcGatewayService`（`kind: 'demo', profile: 'sse'`）；真实模式走原生路由：

```ts
case 'sse':
    service = new SseService(sourceUrl)
    break
```

绑定回调把 `point.value`（经可选 `transform`）写入 `node.data.value`，与其余数据源一致。

## 七、扩展指南

- **支持命名事件**：当前仅 `es.onmessage`（默认 `message` 事件）。若服务端用 `event: alarm` 等命名事件，需 `es.addEventListener('alarm', handler)`。
- **携带鉴权**：原生 `EventSource` 不支持自定义请求头；如需 token，通常通过查询参数 `?token=xxx` 传递（在 URL 拼接处追加）。
- **共享连接**：若希望多点复用一条 SSE 连接，可改为单连接 + 服务端在 JSON 中带 `id` 区分，前端按 `id` 分发到各回调（重构 `sources` 为单实例）。
- **修改模拟数据**：编辑 `gateway-demo` 的 `sse_value()`。

## 八、调试与验证

1. 真实模式：`npm run dev` 后在数据源对话框填入自己的 SSE 接口地址，绑定节点观察推送。
2. 演示模式：`npx tauri dev` 启动桌面应用，新建 SSE 演示数据源（无需地址，`config.demo = true` 标识；可在「演示波形」中更换波形档位），绑定节点后应看到 0~100 锯齿斜升数值。
3. 演示波形逻辑可用 Rust 单测验证：`cargo test -p gateway-demo`。

## 九、关键文件清单

| 文件 | 职责 |
| --- | --- |
| `src/services/SseService.ts` | SSE 服务实现（EventSource 管理、解析，真实模式） |
| `src/services/DataService.ts` | 统一类型定义 |
| `src/composables/useDataService.ts` | 类型路由（演示模式路由到 IPC 网关）与节点绑定 |
| `src/stores/dataSource.ts` | 数据源实例增删改查与持久化 |
| `src-tauri/crates/gateway-demo/src/lib.rs` | 桌面端内置模拟引擎（`profile = Sse` 锯齿波） |
