# HTTP 轮询 数据源 · 开发手册

> 面向维护、调试或扩展 HTTP 轮询数据接入的开发者。数据源类型标识：`http`。

## 一、架构与数据流

HTTP 轮询基于 WebView 原生 `fetch`，真实模式下**无网关**，每个数据点独立一个定时器周期性 GET；演示模式则由桌面端 Rust 网关内置 `DemoAdapter`（默认 `profile = Http`，随机游走；可在数据源对话框「演示波形」中更换）生成数据，经 Tauri IPC 推送（不占用端口）：

```
┌────────────┐  subscribe(pointId)  ┌────────────────────┐  GET url?pointId=x  ┌──────────┐
│ X6 节点     │ ───────────────────▶ │ HttpPollingService │ ──────────────────▶ │ REST 接口 │
│ node.data   │                      │  每点一个 setInterval│ ◀────────────────── │          │
│  .value     │ ◀─────────────────── │                    │   {id,value,...}     └──────────┘
└────────────┘   DataPoint 回调       └────────────────────┘   （默认 2000ms/次）
```

## 二、核心类型定义

复用统一的 `DataPoint` / `IDataService` / `DataBindingConfig`（见 `src/services/DataService.ts`，与 WebSocket 一致）。其中 `DataBindingConfig.interval` 字段即为 HTTP 轮询间隔预留（毫秒）。

数据源实例 `DataSource.type = 'http'`，`config` 通常为空。

## 三、前端 Service 实现

文件：`src/services/HttpPollingService.ts`。

```ts
constructor(url: string, interval = 2000) {
    this.url = url
    this.interval = interval
}

subscribe(pointId: string, callback: DataCallback): void {
    if (!this.callbacks.has(pointId)) this.callbacks.set(pointId, [])
    this.callbacks.get(pointId)!.push(callback)

    if (!this.timers.has(pointId)) {
        const poll = async () => {
            try {
                const sep = this.url.includes('?') ? '&' : '?'
                const resp = await fetch(`${this.url}${sep}pointId=${encodeURIComponent(pointId)}`)
                if (!resp.ok) throw new Error(`HTTP ${resp.status}`)
                const data = await resp.json()
                const point: DataPoint = {
                    id: data.id || pointId,
                    value: data.value ?? data.data,
                    timestamp: data.timestamp || Date.now(),
                    quality: data.quality || 'good',
                }
                for (const cb of this.callbacks.get(pointId) || []) cb(point)
            } catch (e) {
                console.warn('[HTTP] 轮询失败:', e)
            }
        }
        poll()                                            // 立即拉取一次
        const timer = window.setInterval(poll, this.interval)
        this.timers.set(pointId, timer)
    }
}
```

要点：

- **每点独立定时器**：`timers: Map<pointId, number>`，订阅时若该点尚无定时器则创建；`unsubscribe` 清除对应定时器。
- **立即首拉**：订阅后先同步 `poll()` 一次，避免首个间隔内无数据。
- **URL 拼接**：自动判断用 `?` 还是 `&` 追加 `pointId`，并对 pointId 做 `encodeURIComponent`。
- **错误隔离**：单次请求失败仅 `console.warn`，不中断后续轮询。
- `isConnected()` 恒为 `true`（无连接概念）；`disconnect()` 清除全部定时器与回调。

> 注意：`useDataService` 中创建时为 `new HttpPollingService(sourceUrl)`，使用默认 2000ms。如需按数据源自定义间隔，可扩展为读取 `sourceConfig.interval` 传入构造函数。

## 四、数据格式与点ID约定

| 方向 | 内容 | 说明 |
| --- | --- | --- |
| 前端 → 接口 | `GET <url>?pointId=<pointId>` | 轮询请求 |
| 接口 → 前端 | `{ "id"?, "value", "timestamp"?, "quality"? }` | JSON 响应 |

点ID通过查询参数传递，无格式约束。

## 五、内置模拟引擎（演示模式）

Node 版内置模拟接口（原 `mock/server.ts` 的 `startHttpServer()`，监听 `http://localhost:8081`）已移除。演示模式改由桌面端 Rust 网关内置实现：`src-tauri/crates/gateway-demo/src/lib.rs` 的 `DemoAdapter`（`profile = Http`），不监听任何端口、不发 HTTP 请求。

数据生成算法——随机游走，0~100 缓慢漂移，每个 pointId 在适配器内维护独立上一值状态（与原 Node 版 `httpValue()` 行为一致，步进噪声改由哈希派生以保证可复现）：

```rust
/// HTTP 特征：随机游走单步（在上次值基础上步进 ±6，钳制 0~100）
fn http_step(prev: f64, point_id: &str, now_ms: u64) -> f64 {
    let step = (pseudo_noise(point_id, now_ms) - 0.5) * 12.0;
    round1((prev + step).clamp(0.0, 100.0))
}
// 适配器内 walk_state: HashMap<pointId, f64> 保存各点上一轮值
```

## 六、路由与节点绑定机制

`useDataService.ts`：

```ts
// 演示模式（任意协议）与工业协议 → Rust 原生网关（IPC）
if (isDemoSource(sourceType, sourceUrl, sourceConfig) || INDUSTRIAL_TYPES.has(sourceType)) {
    service = new IpcGatewayService(key, buildDeviceConfig(...), ...)  // { kind:'demo', profile:'http' }
}
// 真实模式 → WebView 直连
case 'http':
    service = new HttpPollingService(sourceUrl, sourceConfig?.interval)
    break
```

绑定回调把 `point.value`（经可选 `transform`）写入 `node.data.value`，与其余数据源一致。

## 七、扩展指南

- **自定义轮询间隔**：在 `useDataService` 改为 `new HttpPollingService(sourceUrl, sourceConfig?.interval ?? 2000)`，并在数据源 `config` 中暴露 `interval` 字段、在 `DataSourceDialog` 增加对应输入项。
- **改为 POST 或带鉴权**：在 `poll()` 中替换 `fetch` 选项（`method`、`headers`），例如加入 `Authorization`。
- **批量拉取**：当前是每点一次请求；若接口支持批量，可重构为单定时器拉取多点再分发到各回调。
- **修改模拟数据**：编辑 `src-tauri/crates/gateway-demo/src/lib.rs` 的 `http_step()`（如改为有界正弦、阶梯），`cargo test -p gateway-demo` 验证后重新 `npx tauri dev`。

## 八、调试与验证

1. 演示模式：`npx tauri dev` 启动桌面应用，新建 HTTP 演示数据源并绑定节点，观察 0~100 随机游走数值每秒刷新；Rust 侧日志可见 `kind="demo"` 会话创建记录。
2. 真实模式：接入自己的接口后可用 `curl` 独立验证：`curl "http://192.168.0.30:8000/api/data?pointId=x"` 应返回含 `value` 字段的 JSON。
3. IPC 通道排查：桌面演示模式下若无数据，依次检查 `gateway://status` 事件是否收到 `connected: true`、`invoke('gateway_subscribe')` 是否成功（前端控制台 `[IPC]` 日志）。

## 九、关键文件清单

| 文件 | 职责 |
| --- | --- |
| `src/services/HttpPollingService.ts` | 轮询实现（真实模式：定时器、fetch、解析） |
| `src/services/IpcGatewayService.ts` | IPC 数据服务（演示模式走此通道） |
| `src/services/DataService.ts` | 统一类型定义 |
| `src/composables/useDataService.ts` | 类型路由与节点绑定 |
| `src/stores/dataSource.ts` | 数据源实例增删改查与持久化 |
| `src-tauri/crates/gateway-demo/src/lib.rs` | 内置演示波形引擎（`http_step()` 随机游走算法） |
