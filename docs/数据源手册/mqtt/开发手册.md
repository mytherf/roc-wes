# MQTT 数据源 · 开发手册

> 面向维护、调试或扩展 MQTT 数据接入的开发者。数据源类型标识：`mqtt`。

## 一、架构与数据流

浏览器经 **MQTT over WebSocket**（mqtt.js）连接 broker，点ID即 MQTT 主题，真实模式下**无需自研网关**；演示模式则由桌面端 Rust 网关内置 `DemoAdapter`（`profile = Mqtt`，离散档位）生成数据，经 Tauri IPC 推送（不占用端口）：

```
真实模式：
┌────────────┐  subscribe(topic)   ┌─────────────┐  MQTT over WS (mqtt.js)  ┌────────────┐
│ X6 节点     │ ──────────────────▶ │ MqttService │ ───────────────────────▶ │ MQTT broker │
│ node.data   │                     │             │ ◀════ publish(topic) ═════ │ (EMQX 等)  │
│  .value     │ ◀────────────────── │             │   payload: {value,...}    └────────────┘
└────────────┘   DataPoint 回调      └─────────────┘

演示模式（桌面端）：
┌────────────┐  subscribe           ┌──────────────────┐  invoke gateway_connect   ┌──────────────────┐
│ X6 节点     │ ───────────────────▶ │ IpcGatewayService │ ───────────────────────▶ │ Rust DemoAdapter │
│ node.data   │                      │  (演示模式路由)    │ ◀── gateway://telemetry ── │  profile = Mqtt  │
└────────────┘   DataPoint 回调       └──────────────────┘                            └──────────────────┘
```

## 二、核心类型定义

复用统一的 `DataPoint` / `IDataService` / `DataBindingConfig`（`src/services/DataService.ts`）。数据源实例 `DataSource.type = 'mqtt'`，`config` 通常为空（如需鉴权可扩展）。

## 三、前端 Service 实现

文件：`src/services/MqttService.ts`，基于 `mqtt`（mqtt.js）。

```ts
import mqtt from 'mqtt'

constructor(url: string) {
    this.client = mqtt.connect(url, { reconnectPeriod: 3000, connectTimeout: 5000 })

    this.client.on('connect', () => {
        this.connected = true
        // 重连后重新订阅之前的所有主题
        for (const [topic] of this.callbacks) this.client.subscribe(topic)
    })

    this.client.on('message', (topic, payload) => {
        const data = JSON.parse(payload.toString())
        const point: DataPoint = {
            id: topic,
            value: data.value ?? data.data ?? data,   // 兼容三种载荷
            timestamp: data.timestamp || Date.now(),
            quality: data.quality || 'good',
        }
        for (const cb of this.callbacks.get(topic) || []) cb(point)
    })
}

subscribe(pointId: string, callback: DataCallback): void {
    if (!this.callbacks.has(pointId)) this.callbacks.set(pointId, [])
    this.callbacks.get(pointId)!.push(callback)
    if (this.connected) this.client.subscribe(pointId)
}
```

要点：

- **点ID = 主题**：`callbacks: Map<topic, DataCallback[]>`，订阅即 `client.subscribe(topic)`。
- **自动重连 + 重订阅**：`reconnectPeriod: 3000`；`connect` 事件里遍历 `callbacks` 重新订阅。
- **载荷兼容**：`value ?? data ?? 整个载荷`，既能解析结构化 JSON，也能接收纯数值载荷。
- **通配符**：mqtt.js 支持 `+`/`#`；但回调按「精确 topic」匹配 `callbacks`，通配符订阅收到的具体主题需在 `callbacks` 中有对应键才会分发（见扩展指南）。
- `disconnect()` 调用 `client.end(true)` 强制断开并清空回调。

## 四、数据格式与点ID约定

| 方向 | 内容 | 说明 |
| --- | --- | --- |
| 前端 → broker | `SUBSCRIBE <topic>` | 订阅主题（mqtt.js 封装） |
| 设备 → broker → 前端 | `PUBLISH <topic>` payload `{"value", "timestamp"?, "quality"?}` | 数据消息 |

点ID遵循 MQTT 主题规范（`/` 分层，支持 `+`/`#` 通配）。

## 五、内置模拟引擎（演示模式）

Node 版内置模拟 broker（原 `mock/server.ts` 的 `startMqttServer()`，aedes，TCP 1883 / WS 8083）已移除，`aedes`/`ws` 等 devDependencies 一并清理。演示模式改由桌面端 Rust 网关内置实现：`src-tauri/crates/gateway-demo/src/lib.rs` 的 `DemoAdapter`（`profile = Mqtt`），不监听任何端口、不建立 MQTT 连接，由 gateway-engine 会话任务按轮询周期读取后经 Tauri IPC 事件推送。

数据生成算法——离散设备档位，在 `[0, 25, 50, 75, 100]` 间每 3 秒窗口步进切换（与原 Node 版 `mqttValue()` 行为一致）：

```rust
const MQTT_STATES: [f64; 5] = [0.0, 25.0, 50.0, 75.0, 100.0];
fn mqtt_value(point_id: &str, now_ms: u64) -> f64 {
    let step = now_ms / 3000;
    MQTT_STATES[((step + hash_u64(point_id)) % MQTT_STATES.len() as u64) as usize]
}
```

## 六、路由与节点绑定机制

`useDataService.ts`：演示模式（`isDemoSource` 判定）在 Tauri 桌面运行时统一路由到 `IpcGatewayService`（`kind: 'demo', profile: 'mqtt'`）；真实模式走原生路由：

```ts
case 'mqtt':
    service = new MqttService(sourceUrl)
    break
```

绑定回调把 `point.value`（经可选 `transform`）写入 `node.data.value`，与其余数据源一致。

## 七、扩展指南

- **鉴权接入**：在 `mqtt.connect(url, { username, password, clientId })` 中补充；可将这些字段加入数据源 `config`，由 `useDataService` 透传给 `MqttService` 构造函数。
- **通配符订阅分发**：当前 `message` 回调按精确 topic 命中 `callbacks`。若需让通配符订阅（如 `factory/#`）把具体主题分发给订阅者，可在 `message` 中对 `callbacks` 的键做 MQTT 主题匹配（实现 `+`/`#` 匹配算法）后分发。
- **QoS / retain**：订阅时 `client.subscribe(topic, { qos: 1 })`；发布侧按需设置 retain。
- **修改模拟数据**：编辑 `gateway-demo` 的 `mqtt_value()` 或 `MQTT_STATES` 档位。

## 八、调试与验证

1. 真实模式：`npm run dev` 后在数据源对话框填入自己 broker 的 WebSocket 接入点，绑定主题观察推送。
2. 演示模式：`npx tauri dev` 启动桌面应用，新建 MQTT 演示数据源（地址自动预填为演示标识 `ws://localhost:8083`），绑定具体主题后应看到 `0/25/50/75/100` 离散档位每约 3 秒切换。
3. 演示波形逻辑可用 Rust 单测验证：`cargo test -p gateway-demo`。

## 九、关键文件清单

| 文件 | 职责 |
| --- | --- |
| `src/services/MqttService.ts` | MQTT 服务实现（mqtt.js 连接、订阅、解析，真实模式） |
| `src/services/DataService.ts` | 统一类型定义 |
| `src/composables/useDataService.ts` | 类型路由（演示模式路由到 IPC 网关）与节点绑定 |
| `src/stores/dataSource.ts` | 数据源实例与演示标识地址 |
| `src-tauri/crates/gateway-demo/src/lib.rs` | 桌面端内置模拟引擎（`profile = Mqtt` 离散档位） |
