# roc-wes 文档中心

本目录是 roc-wes 项目的文档总入口。所有面向用户与开发者的正式文档集中于此；根目录 `roc-wes 数据源手册.md` 为项目门面摘要。

## 目录结构

```
docs/
├── README.md                    # 文档中心入口（本文件）
├── 用户使用手册/
│   └── 用户手册.md               # 画面编辑、数据源、节点操作手册、运行态
├── 开发指南/
│   ├── 开发指南.md               # 环境搭建、开发命令、协议扩展点
│   ├── roc-wes 画布编辑器开发流程.md               # 拖拽 → 画布 → Store → 属性面板数据流
│   ├── tauri-目标架构.md         # 桌面版 IPC-first 架构与实现状态
│   ├── tauri-迁移方案.md         # 已归档：迁移期决策记录
│   └── tauri-*.png / .mermaid    # 架构图 / 时序图（源文件与 PNG 同名同目录）
├── 数据源手册/
│   ├── README.md                # 数据源手册入口
│   └── {websocket,http,sse,mqtt,s7,opc,modbus}/
│       ├── 用户使用手册.md        # 用户视角：配置、点位、演示/真实模式
│       └── 开发手册.md            # 开发视角：接口、扩展点、实现细节
├── articles/                    # 技术系列文章（含 images/ 配图）
└── 产品需求/                     # 产品分析与需求路线图
```

## 文档地图

| 文档 | 读者 | 内容 |
| --- | --- | --- |
| [用户手册](用户使用手册/用户手册.md) | 组态 / 运维人员 | 画面编辑、数据源配置、**各类节点操作手册**、演示与真实模式、运行态使用 |
| [开发指南](开发指南/开发指南.md) | 开发者 | 环境搭建、开发命令、测试检查、协议扩展点、关键机制 |
| [数据源手册](数据源手册/roc-wes 数据源手册.md) | 用户 + 开发者 | 7 种协议各一份「用户使用手册 + 开发手册」 |
| [Tauri 目标架构](开发指南/tauri-目标架构.md) | 架构 / 核心开发 | 桌面版 IPC-first 架构、Rust workspace 设计与实现状态 |
| [编辑器数据流（开发流程）](开发指南/roc-wes 画布编辑器开发流程.md) | 前端开发 | 拖拽 → 画布 → Store → 属性面板的完整数据流剖析 |
| [技术系列文章](articles/01-项目总览与架构设计.md) | 全员 | 5 篇架构解析（总览 / 画布编辑器 / 多协议接入 / 路线编辑器 / 运行态） |
| [与开源 SCADA 平台 FUXA 对比分析](articles/06-与开源SCADA-FUXA对比分析.md) | 产品 / 架构 | 与 FUXA 的功能与架构逐项对照、能力矩阵与借鉴建议 |
| 架构图 | 全员 | [tauri-目标架构图.png](开发指南/tauri-目标架构图.png)、[tauri-架构图.png](开发指南/tauri-架构图.png)、[tauri-时序图.png](开发指南/tauri-时序图.png) |
| [Tauri 迁移方案](开发指南/tauri-迁移方案.md) | — | **已归档**：迁移期决策记录，落地方案以「目标架构」为准 |

> 根目录另有 `articles/`（技术系列文章，随代码演进维护）与 `产品需求/`（一次性分析产物，不在常规维护范围内）。

## 文档分工与维护约定

- **README.md（根）**：门面摘要，只讲「是什么 + 怎么跑起来」，细节一律链接到 docs/。
- **用户手册**：只讲使用，不出现源码路径；协议相关的深入内容链接到对应数据源手册。
- **开发指南**：跨模块的机制与流程；单协议的实现细节在各协议「开发手册」里。
- **数据源手册**：每协议两份，用户 / 开发视角分离；新协议落地时必须同步补齐两份手册。
- **架构图**：mermaid 源文件与 PNG 同名同目录，改图时两者同步更新。
- **同步规则**：涉及命令、端口、协议支持范围的代码变更，必须同步更新本目录受影响文档（验收基线：`docs/` 内不出现已删除的目录、脚本或端口）。

## 统一规划（路线图）

以 [Tauri 目标架构 · 第 8 节](开发指南/tauri-目标架构.md#8-当前实现状态与后续路线) 为准，当前快照：

### 已完成

- IPC-first 桌面架构：Cargo workspace（core / engine / modbus / demo）+ Tauri 壳 + 前端平台接缝
- Modbus TCP 桌面直连（tokio-modbus）、全协议演示模式（DemoAdapter）
- 引擎会话机制：轮询下限 200ms、失败指数退避重连（2s→30s）、状态事件去抖
- 监控探针桌面适配（`mon:` 独立会话）、集成测试与 clippy 零警告基线
- 代码清理：Node 网关与工业 mock 桥接退役、依赖精简、S7 slot 默认值统一
- 应用图标与 NSIS 打包配置、`npx tauri dev` 开发链路
- 持久化升级：tauri-plugin-fs 全部工程数据落盘为应用配置目录 JSON 文件（原子写入），全面替代 localStorage / sessionStorage
- 全协议 Rust 网关统一：Web 协议（WebSocket/HTTP/SSE/MQTT）适配器 crate 上线，真实模式与演示模式统一走 IPC
- S7 / OPC UA 适配器上线：`gateway-s7`（snap7-client，nodes7 点位合同，snap7-server 往返测试）+ `gateway-opcua`（opcua crate 匿名轮询），factory 最后两个 Unsupported 分支补齐，7 类协议真实模式全通

### 后续（按风险排序）

1. **打包完善**：NSIS 安装器调优、（可选）tauri-plugin-updater 自动更新
2. **日志落盘**：tracing-appender 滚动文件（AppData）

### 已知限制（现状声明）

- 应用为纯桌面版（Tauri），统一以 `npx tauri dev` 启动；ws/http/sse/mqtt 需外部服务或使用演示模式。
- 7 类协议真实模式均已上线（S7 需真实 PLC、OPC UA 需真实服务器）；S7/OPC UA 构建依赖仓库根 `.cargo/config.toml` 中的 OpenSSL 静态链接配置。
- 未绑定数据源的节点保持静态值，不产生任何模拟数据变化；需要实时数据请显式绑定数据源。
